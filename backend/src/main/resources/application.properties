# Database Configuration
quarkus.datasource.db-kind=mysql
quarkus.datasource.username=root
quarkus.datasource.password=AniLakh4508@!
quarkus.datasource.jdbc.url=jdbc:mysql://localhost:3306/drug_management?useSSL=false&allowPublicKeyRetrieval=true&serverTimezone=UTC

# Hibernate Configuration
quarkus.hibernate-orm.database.generation=update
quarkus.hibernate-orm.log.sql=false
# MySQL specific dialect
quarkus.hibernate-orm.dialect=org.hibernate.dialect.MySQLDialect

# JWT Configuration (Disabled for now - will implement later)
# mp.jwt.verify.publickey.location=classpath:/publicKey.pem
# mp.jwt.verify.issuer=https://drugmanagement.com
# Security Configuration (Disabled for now)
quarkus.security.users.embedded.enabled=false

# REST API Configuration
quarkus.http.cors=true
quarkus.http.cors.origins=http://localhost:5173,http://localhost:3000
quarkus.http.cors.headers=accept,authorization,content-type,x-requested-with

# Logging
quarkus.log.level=INFO
quarkus.log.category."com.drugmanagement".level=DEBUG

# OpenAPI
quarkus.smallrye-openapi.path=/api-docs
